{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // No react here — the root default lists it and this member never renders JSX.
    "types": ["bun"]
  },
  "include": ["src", "test"]
}
